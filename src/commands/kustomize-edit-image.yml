description: >
# What will this command do?
# Descriptions should be short, simple, and clear.
parameters:
  path:
    type: string
    default: "."
    description: ""
  from:
    type: string
    description: ""
  to:
    type: string
    description: ""
steps:
  - install-kustomize
  - run:
      name: kustomize edit set image
      command: kustomize edit set image << parameters.from >>=<< parameters.to >>
